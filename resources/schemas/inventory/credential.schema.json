{
  "type": "object",
  "version": "1.0.0",
  "title": "Credentials",
  "description": "Credentials inventory item",
  "properties": {
    "type": {
      "type": "string",
      "title": "Credentials Type",
      "enum": [
        "ssh",
        "api_key",
        "username_password"
      ]
    }
  },
  "required": [
    "type"
  ]
}