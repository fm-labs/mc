{
  "type": "object",
  "title": "Container Node",
  "description": "A host node that runs docker with the MissionControl agent installed. This node can be used to run container workloads and collect inventory data about the containers running on it.",
  "version": "1.0.0",
  "$icon": "server",
  "properties": {
    "url": {
      "type": "string",
      "title": "MissionControl API Base URL"
    },
    "autoconnect": {
      "type": "boolean",
      "title": "Auto-connect",
      "default": false
    }
  },
  "required": [
    "url"
  ]
}