{
  "type": "object",
  "title": "Container Registry",
  "description": "Container registry inventory item",
  "version": "1.0.0",
  "properties": {
    "registry_url": {
      "type": "string",
      "title": "Registry URL Prefix",
      "description": "The URL prefix for the container registry including the protocol (e.g. https://docker.io/)"
    },
    "username": {
      "type": "string",
      "title": "Username",
      "default": ""
    }
  },
  "required": [
    "registry_url"
  ]
}