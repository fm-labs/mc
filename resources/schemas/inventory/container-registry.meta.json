{
  "actions": [
    {
      "id": "login",
      "name": "Login",
      "description": "Authenticate to the container registry using the provided credentials",
      "type": "form",
      "inputSchema": {
        "type": "object",
        "properties": {
          "password": {
            "type": "string",
            "title": "Password",
            "description": "Password or token to authenticate to the container registry",
            "default": "",
            "format": "password"
          },
          "target_node": {
            "type": "string",
            "title": "Target Node",
            "description": "If specified, perform the login on this remote node instead of the local node."
          },
          "store": {
            "type": "boolean",
            "default": true,
            "description": "Store credentials in the system's secure credential store"
          }
        }
      }
    }
  ]
}