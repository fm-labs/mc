import {SatelliteIcon,} from "lucide-react";
import { MC_BRAND_NAME } from "@/constants.ts";

export const appSidebarData: any = {
    teams: [
        {
            name: MC_BRAND_NAME,
            logo: SatelliteIcon,
            plan: "Pro",
        },
    ],
};
